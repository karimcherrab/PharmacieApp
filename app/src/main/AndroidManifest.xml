<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="my.gamekarimbull_cow.pharmacie">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Pharmacie">
        <activity android:name=".LivreurActivity"></activity>
        <activity android:name=".commande_detail_Activity" />
        <activity android:name=".Modifier_Profile_Activity" />
        <activity android:name=".PannerActivity" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Account.SignUpActivity" />
        <activity android:name=".Account.LoginActivity" />
        <activity
            android:name=".HomeActivity"
            android:exported="true" />
    </application>

</manifest>